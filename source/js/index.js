
/**
 * Initializing Randomizer and setting the most
 * used random compartments
 */
// var randomizer = new Randomizer();
// randomizer.randomNumberKeepAmount = 5;
// randomizer.RefillRandomNumbers(1, 16);
// randomizer.RefillRandomNumbers(1, 10);
// randomizer.RefillRandomNumbers(1, 100);
// randomizer.RefillRandomNumbers(1, 4);
// randomizer.RefillRandomNumbers(1, 2);

/**
 * Initializing App and setting it
 */
var app = new App();

